/**
 * İletişim — placeholder while the project-block gate is being verified.
 * Replaced fully in §H.
 */
export function Contact() {
  return (
    <section id="contact" className="mx-auto w-full max-w-6xl scroll-mt-24 px-6 py-24">
      <p className="eyebrow">İletişim</p>
      <p className="mt-3 text-muted">§H burada gelecek.</p>
    </section>
  );
}
